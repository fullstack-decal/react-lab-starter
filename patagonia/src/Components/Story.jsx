import Story from 'react'
import { Box } from '@chakra-ui/react'

const Story = ({ title, author, length, image }) => {
    return (
        <Box>
            test
        </Box>
    )
}

export default Story